meta {
  name: Unenroll User
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/courses/{{courseId}}/enroll/{{enrollUserId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars {
  enrollUserId:
}
