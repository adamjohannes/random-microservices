meta {
  name: Update Account
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/api/v1/accounts/{{accountId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Adam J",
    "email": "adam.j@example.com"
  }
}
